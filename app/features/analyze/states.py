from aiogram.fsm.state import StatesGroup, State


# =========================
# ANALYSIS FSM (КАНОН)
# РОЛЬ: АНАЛИЗ ГОТОВОГО ДОКУМЕНТА
# =========================
class AnalysisStates(StatesGroup):
    # 1️⃣ Тип документа
    # (договор / соглашение / акт / претензия / иной)
    document_type = State()

    # 2️⃣ Сам документ
    # (текст документа целиком или ключевые фрагменты)
    document_text = State()

    # 3️⃣ Цель анализа
    # (риски / налоги / ответственность / расторжение / защита интересов)
    goals = State()

    # 4️⃣ Контекст
    # (до подписания / уже подписан / спор / переговоры / проверка)
    context = State()

    # 5️⃣ Подтверждение
    confirm = State()
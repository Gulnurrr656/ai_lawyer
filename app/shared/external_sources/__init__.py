"""External official-source clients (Adilet, etc.).

These clients are *fallback only*. RAG is always primary; nothing fetched
here is treated as a verified legal basis until it has been ingested into
``rag/`` through the safe staging pipeline.
"""
